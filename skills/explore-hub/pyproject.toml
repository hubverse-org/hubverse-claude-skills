[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["scripts"]

[project]
name = "explore-hub"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "hubdata",
    "polars",
    "pyarrow",
    "pyyaml",
]
